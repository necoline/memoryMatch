// onclick the card flips
//if match the image stays
  //if match add points and adds attemp #
//else image returns
  //else adds attempt number
//onclick 'new game' button
  //clears history
  // randomizes imgs

function firstbutton() {
	document.getElementById("pic1").innerHTML = 1;

}
